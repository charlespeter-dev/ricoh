{
  "name": "web-refresh",
  "version": "1.0.0",
  "description": "ricoh web refresh",
  "main": "index.js",
  "scripts": {
    "dev": "npx sass --quiet --watch assets/scss:assets/css",
    "build": "npx sass --quiet --style=compressed assets/scss:assets/css",
    "serve": "npx live-server --port=8080 --no-css-inject"
  },
  "author": "charles",
  "license": "ISC",
  "devDependencies": {
    "bootstrap": "^5.3.8",
    "live-server": "^1.2.2",
    "sass": "^1.94.2"
  }
}
